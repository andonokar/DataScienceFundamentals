import numpy as np

# converting list into array
arr = np.array([1,2,3,4])

# start matrix with zeroes
zeroes = np.zeros((3,3))

# start matrix with ones
ones = np.ones((2,4))

# generate array similar to range builtin
range_array = np.arange(1, 10 , 2)

# spread evenly between start and stop by a value to spread
linspace_array = np.linspace(0, 1, 5)

