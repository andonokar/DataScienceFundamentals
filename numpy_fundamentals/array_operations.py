import numpy as np

a = np.array([1,2,3])
b = np.array([4,5,6])

# add arrays
print(a + b)

# multiply arrays
print(a * b)

# divide arrays
print(a / b)

# square root
print(np.sqrt(a))

# sum array
print(np.sum(a))

# mean
print(np.mean(a))

# max
print(np.max(a))